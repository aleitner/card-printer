<style>
  .reaction-card-type {
    color: red;
  }
  </style>
  
  <div class="card reaction-card" data-card-name="<%= cardName %>">
    <div class="card-title"><%= card.name %></div>
    <div class="card-type reaction-card-type">Reaction</div>
    <div class="card-description"><%= card.description %></div>
    <div class="card-modifier">Accuracy Modifier: <%= card.modifier %></div>
  </div>